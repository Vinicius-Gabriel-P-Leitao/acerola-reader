<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Acerola">
        <activity
            android:name=".ui.feature.home.activity.HomeActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.Acerola">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".ui.feature.config.activity.ConfigActivity"
            android:label="@string/label_config_activity"
            android:theme="@style/Theme.Acerola" />

        <activity
            android:name=".ui.feature.history.activity.HistoryActivity"
            android:label="@string/label_history_activity"
            android:theme="@style/Theme.Acerola" />

        <activity
            android:name=".ui.feature.chapters.activity.ChaptersActivity"
            android:label="@string/label_chapters_activity"
            android:theme="@style/Theme.Acerola" />
    </application>

</manifest>