#[cfg(test)]
mod tests {
    use pdfium::PdfiumDocument;

    #[test]
    fn introspect_page() {
        let _doc: PdfiumDocument;
        // let _page = _doc.page(0).unwrap();
        // let _ = _page.does_not_exist();
    }
}
