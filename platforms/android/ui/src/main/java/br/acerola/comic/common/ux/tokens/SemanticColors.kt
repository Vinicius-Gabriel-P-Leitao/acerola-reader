package br.acerola.comic.common.ux.tokens

import androidx.compose.ui.graphics.Color

// Cores fixas propositalmente: mantêm o significado (ex.: estrela = nota) reconhecível
// em todos os temas, em vez de variar com o ColorScheme ativo.
object SemanticColors {
    val Rating = Color(0xFFFFC107)
}
